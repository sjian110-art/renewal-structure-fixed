# KAERI 수정본 사용 안내

## 실행

1. ZIP을 새 폴더에 압축 해제합니다. 기존 폴더 위에 섞어서 덮어쓰지 않는 편이 안전합니다.
2. VS Code 또는 안티그래비티에서 `renewal-fixed` 폴더를 엽니다.
3. `index.html`을 Live Server로 실행합니다. npm 설치는 필요하지 않습니다.
4. 기존 화면이 캐시에 남으면 Ctrl+Shift+R로 새로고침합니다.

원본 이미지, 참고 시안, 기타 원본 폴더는 유지했습니다. 변경 전의 HTML/CSS/JS는 `backup-before-repair`에 별도로 보관했습니다. 이 백업의 index.html은 독립 실행본이 아니라 코드 비교·복구용입니다.

## 실제로 발견한 오류

- 원자 2~6의 부모에 `visibility: hidden`을 설정한 뒤 opacity만 올려 계속 보이지 않았습니다.
- 1번 원자는 `opacity: 1 !important`로 강제 노출되어 다른 단계와 충돌했습니다.
- 배경별 UI와 다이얼을 GSAP `.call()`의 부수 효과로 변경해 역방향/빠른 탐색 시 화면 위치와 상태가 어긋날 수 있었습니다.
- 원호 SVG와 숫자 좌표가 서로 다른 중심과 반지름을 사용했습니다. SVG 경로도 단일 원호가 아니었습니다.
- 여러 스토리 이미지가 이전 이미지를 지우지 않은 상태에서 동시에 표시됐습니다.
- ‘핵분열’이라고 주석에 적힌 코드는 실제로 4.8배 확대 후 페이드아웃뿐이었습니다.
- 없는 `icon-arrow-down-dark.png`를 참조했습니다.
- 작은 원의 CSS 애니메이션과 GSAP가 같은 transform을 제어했습니다.
- 빈 공간 클릭 영역 위에 다른 컨테이너가 있어서 클릭이 가로막혔습니다.
- 존재하지 않는 상세 경로를 connected: true로 표시한 메뉴가 있었습니다.

## 수정 내용

- 외부 GSAP 로딩에 의존하던 동작을 native scroll + requestAnimationFrame으로 정리했습니다. 기존 외부 라이브러리 기반 타임라인은 제거했습니다.
- 한 개의 연속 장면 위치에서 배경, 작품, 다이얼, 설명을 계산합니다. 이전 콜백 실행 여부에 의존하지 않습니다.
- 흰 원자 6개를 모두 사용하고 맥박/부유를 적용했습니다. 장면은 충분히 유지한 뒤 다음 단계와 짧게 연결됩니다.
- 원자 마지막에 결정론적인 입자 분리·확산 장면을 추가했습니다. 스크롤을 되돌리면 같은 경로로 돌아옵니다.
- 검은 장면은 WebGL로 국소적인 에너지 흐름을 적용합니다. 의료기기 등 구조물 중심은 보호하고 주변에 움직임을 집중합니다.
- 검은 배경은 #000으로 복원했습니다. 우측 탭의 회색 PNG는 배경 테마와 분리했습니다.
- 다이얼 원호와 숫자는 같은 SVG 좌표와 실제 원을 공유합니다.
- black1~3에는 다이얼이 없고, black4/7/11에는 다이얼만, black5/8/12에는 해당 설명이 함께 나옵니다.
- black23에 해당하는 마무리 문구를 복원했습니다.
- 메뉴는 호버=색/설명, 클릭=확대/밀림/블러, 자세히 보기=별도 버튼으로 구분했습니다.
- 선택된 하단 원은 안쪽으로 이동하며 커집니다. 빈 공간/Esc/스크롤로 해제합니다.
- 작은 원의 부유, 클릭 시 밀림, 스크롤 퇴장은 별도 래퍼로 분리했습니다.
- 확대 중 브라우저 스크롤 앵커가 화면을 이동시켜 선택을 풀던 문제를 차단했습니다.
- 화면에 없는 상세 페이지는 준비 안내를 표시합니다. 햄버거는 현재 구현된 원형 메뉴 구간으로 이동합니다.
- 다음 섹션은 콘텐츠를 임의로 만들지 않은 빈 연결 구간입니다.

## 수정 파일

- index.html
- css/style.css
- js/script.js: 장면, 다이얼, 메뉴 동작
- js/menu-data.js: 원별 명칭/색상/크기/설명 (기존 데이터 분리)
- js/energy-renderer.js: 국소적인 GPU 움직임
- package.json / preview-server.cjs: 설치 없이 `npm run dev`로도 실행 가능한 보조 서버

## 현재 한계 — 완성된 모핑으로 오해하지 않도록

원본은 중심·궤도·구조물·주변 빛이 합쳐진 정지 PNG입니다. 이 수정본은 국소 변형과 입자 분리 연출을 추가한 복구본이며, 의료기기→우주→손 등 서로 다른 형상이 실제로 같은 입자로 재구성되는 고급 모핑은 아닙니다. 장면 사이에는 짧은 디졸브가 남아 있습니다. 손 자세의 연속 변화와 파란 입자가 주황 손에 물리적으로 흡수되는 완성 모션은 별도의 영상/연속 프레임 또는 형상 데이터를 제작해야 합니다.

WebGL을 사용할 수 없는 환경에서는 원본 이미지 전환으로 대체됩니다. 축소 모션 설정에서는 지속적인 맥박과 부유를 줄입니다. 실제 휴대폰 레이아웃을 새로 디자인하지 않았으며 데스크톱 시안을 기준으로 비율을 유지합니다. 한국어 폰트는 기존 Google Fonts를 유지하므로 인터넷이 없을 때 대체 폰트가 보일 수 있습니다.

## 브라우저 검증

Chrome의 실제 화면에서 첫 화면 이미지와 어두운 UI, 흰 원자 궤도 단계, 검은 첫 장면의 다이얼 숨김, 의료기기 장면의 원호/01/설명, 입자 분리 장면, 원형 메뉴, 하단 보도자료 원의 확대/자세히 보기 안내, Esc 및 빈 공간 선택 해제를 확인했습니다. JavaScript 문법과 HTML의 로컬 파일 경로, PNG 파일 무결성도 확인했습니다.

정밀한 최종 디자인 승인이나 모든 기기에서의 성능 보증은 아닙니다. 다음 작업에서는 이 상태를 기준으로 한 구간씩 수정하고, 특히 같은 요소의 transform을 여러 애니메이션에서 동시에 바꾸지 마세요.
